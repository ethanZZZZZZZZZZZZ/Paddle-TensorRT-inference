# Models

Place exported Paddle Inference model files here for local validation.

Example expected by `configs/paddle.yaml`:

```text
models/inference.pdmodel
models/inference.pdiparams
```

This repository does not include model weights or benchmark outputs.
